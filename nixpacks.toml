[phases.setup]
nixPkgs = ["python312", "gcc", "cargo", "rustc", "openssl", "pkg-config"]

[phases.install]
cmds = [
  "pip install --upgrade pip setuptools wheel",
  "pip install pydantic-core==2.14.6 --no-build-isolation",
  "pip install -r requirements.txt"
]

[start]
cmd = "python bot.py"